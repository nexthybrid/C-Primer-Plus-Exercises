#include <stdio.h>

/* integer math, multiplication */

int main()
{
    int toes = 10;
    printf("toes = %i\n", toes);
    // twice toes
    printf("twice toes = %i\n", (2*toes));
    // toes squared
    printf("toes squared = %i\n", (toes * toes));
    return 0;
}